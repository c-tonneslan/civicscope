fastapi==0.138.0
pytest==9.1.1
httpx==0.28.1
uvicorn[standard]==0.49.0
pydantic-settings==2.14.2
argon2-cffi==25.1.0
email_validator==2.3.0

# --- Civic-intelligence slice (feat/civic-intel-slice) ---
# Postgres driver (psycopg 3, binary wheels = no local libpq build).
psycopg[binary]==3.2.3
# Connection pool (ships separately from psycopg core).
psycopg-pool==3.2.4
# pgvector Python helpers (adapt list <-> vector type for psycopg).
pgvector==0.3.6
# Local ONNX embeddings (BAAI/bge-small-en-v1.5, 384-dim, no torch).
fastembed==0.5.1
# Anthropic Messages API client (optional; only if llm_provider="anthropic").
anthropic==0.42.0

# --- Test-only (civic slice exhaustive suite) ---
# Property-based testing for the combinatorial input space (RRF, citations).
hypothesis==6.122.3
# Coverage reporting for the civic modules.
pytest-cov==6.0.0